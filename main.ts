const testObj = {
  pilot: ["Han Solo", "Lando Calrissian"],
  copilot: ["Chewbacca", "Rey"],
  ship: "Falcon",
  speed: "1.5c"
}

function generatePermutations (currObj : Object): Object[] {
  var resultsArr : Object[];
  //stop recursion when the current object is empty
  if (Object.keys(currObj).length == 0) {
    //stop processing - nothing here
  }

  return resultsArr;
};
